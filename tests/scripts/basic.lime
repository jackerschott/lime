#   this is an example lime comment
//  this is another example comment

/* everything inside a multiline comment should be ignored
base = create_layer()
base = apply_brush(base, 'darkred', 0.5)
base = gaussian_blur(base, 0.5, 0.5)
*/

base = create_layer()
base = apply_brush(base, 'darkred', 0.5)
base = gaussian_blur(base, 0.5, 0.5)

base = open('images/lime.jpg')
base = gaussian_blur(base, 0.5, 0.5)
save(base, 'images/output.jpg')

render(base)